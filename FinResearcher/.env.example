# FinResearcher environment config
# Copy to .env and fill in values

DEBUG=true

# Kafka (optional — set to true if Kafka is running)
KAFKA_ENABLED=false
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_STOCK_TOPIC=stock_predictions
